# 複製成 .env（.env 不進版控）
#
# VITE_API_BASE（runtime，Vite 會 inline 進 bundle）
#   本機留空 → 走 Vite proxy 的 /api；正式設成後端網址，例如 https://api.你的網域
VITE_API_BASE=
#
# 註：prebuild 抓題目用的 API_URL（scripts/fetch-survey.mjs）是「純 node 環境變數」，
#     不會從這個 .env 讀取。本機不設 → 預設 http://localhost:8080。
#     正式環境（Cloudflare Pages）在「建置環境變數」同時設這兩個：
#       VITE_API_BASE = https://api.你的網域
#       API_URL       = https://api.你的網域
#     詳見 repo 根目錄 DEPLOY.md。
